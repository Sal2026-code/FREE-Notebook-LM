import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.busytamarincrawl',
  appName: 'busy-tamarin-crawl',
  webDir: 'dist'
};

export default config;
